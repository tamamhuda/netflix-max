[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @netflix-max/api build"

[deploy]
startCommand = "node apps/api/dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
