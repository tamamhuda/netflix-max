export enum SubscriptionPlan {
  BASIC = 'basic',
  STANDARD = 'standard',
  PREMIUM = 'premium',
  MAX = 'max',
}

export enum ContentBrand {
  NETFLIX = 'netflix',
  HBO = 'hbo',
  DC = 'dc',
  WB = 'wb',
}

export enum MediaType {
  MOVIE = 'movie',
  TV = 'tv',
}
