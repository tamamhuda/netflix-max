# Skip postinstall scripts that require native bindings
[phases.install]
cmds = ["pnpm install --ignore-scripts"]

[phases.build]
cmds = ["pnpm --filter @netflix-max/api build"]

[start]
cmd = "node apps/api/dist/main.js"

